AzureOrchestrator

This project showcases how to orchestrate two Azure Function Apps using Azure Durable Functions.

The workflow is coordinated by an orchestrator function, which manages the execution and sequencing of tasks.

One of the key components is the language recognition function, adapted from a classmate's work. 

It processes voice-to-text input, identifies the language being used, and returns a corresponding response in that language.
